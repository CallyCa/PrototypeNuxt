language: node_js
node_js:
- lts/*
os:
- linux
dist: focal
branches:
  only:
  - main
  - master
cache: yarn
script:
- yarn lint --fix
deploy:
  provider: heroku
  api_key:
    secure: $HEROKU_AUTH_TOKEN
  app: $APP
  on:
    repo: $REPOSITORY
